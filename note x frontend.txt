Nel file api.js del tuo frontend, non esiste ancora una funzione per richiedere la posizione via API. Tuttavia, il backend ora supporta:

http
Copia
Modifica
POST /request-position
✅ Come integrarla in api.js
Aggiungi questa funzione in fondo al file api.js:

javascript
Copia
Modifica
// Richiede la posizione da un nodo specifico
export async function requestNodePosition(node_id) {
  const res = await fetch(API + "/request-position", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ node_id }),
  });
  if (!res.ok)
    throw new Error(`Fetch error ${res.status} for ${API}/request-position`);
  return res.json();
}
🧠 Come usarla nel tuo codice Vue/JS
Esempio in un componente:

javascript
Copia
Modifica
import { requestNodePosition } from "../lib/api";

// Quando clicchi un bottone o simile
await requestNodePosition("974167536");